name: Snyk Scan Master
on:
  push:
    branches: master

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
    steps:
      - name: NPM Install
        run: npm install
      - name: Install Snyk
        run: sudo npm install -g snyk
      - name: ECHO TEST
        run: echo "Test"
      - name: ECHO "$SNYK_TOKEN"
        run: echo "$SNYK_TOKEN"
      - name: Snyk Authenticate
        run: snyk auth "$SNYK_TOKEN"
      - name: Snyk Test
        run: snyk test
